{
    "name": "@repo/tests",
    "version": "2.1.0",
    "private": true,
    "scripts": {
        "test": "vitest run"
    },
    "devDependencies": {
        "spreadorm": "workspace:*",
        "vitest": "^3.1.4"
    }
}
